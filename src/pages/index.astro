---
import Page from "../layouts/Page.astro";
import { EPageType, getProps } from "../services/content/getProps";

const props = await getProps({pageType: EPageType.Index});

if (props.is404) return Astro.redirect("/404");
---
<Page {...props}/>

